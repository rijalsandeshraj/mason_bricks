name: mason_bricks
description: Pubspec file for working with mason bricks.

publish_to: "none"

version: 1.0.0

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.5
  equatable: ^2.0.5
  flutter_bloc: ^8.1.1
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
